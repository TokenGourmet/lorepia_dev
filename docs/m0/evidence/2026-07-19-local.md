# M0 scaffold local evidence receipt — 2026-07-19

## Subject

- Feature commit: `3434dbe103f6cd77bb772c874a4ef00098612d6f`
- Branch: `agent/m0-product-scaffold`
- Scope: local macOS checks, packaged macOS runtime, and iOS simulator compile
- Not covered: Android compile, Linux/Windows compile, physical mobile smoke,
  signing, store submission, benchmark baseline, or M-1 exit

## Host and tools

```text
macOS 26.5.2 (25F84), arm64
Node v22.23.0
npm 10.9.8
rustc 1.97.1 (8bab26f4f 2026-07-14)
cargo 1.97.1 (c980f4866 2026-06-30)
Xcode 26.6 (17F113)
CocoaPods 1.16.2
```

## Contract and build checks

The following passed against the feature commit and its committed lockfiles:

```text
npm ci
npm test                                      15 tests passed
npm audit --audit-level=moderate              passed; 3 low dev findings
npm audit --omit=dev --audit-level=low        0 production findings
npm run check                                 0 errors, 0 warnings
npm run build                                 passed
cargo fmt --all -- --check                    passed
cargo test --locked --workspace --all-targets 3 tests passed
cargo clippy --locked --workspace --all-targets -- -D warnings
cargo check --locked --workspace --all-targets
npm run tauri build -- --debug --bundles app --ci
npm run tauri ios build -- --debug --target aarch64-sim --ci --no-sign
git diff --cached --check
```

The final native outputs were:

```text
target/debug/bundle/macos/LorePia.app
apps/desktop-mobile/src-tauri/gen/apple/build/arm64-sim/LorePia.app
```

The iOS build created an ARM64 simulator bundle. It was not launched. A repeat
build encountered Tauri's `Directory not empty` rename error while the ignored
prior `gen/apple/build` output existed; moving that generated directory aside
and rebuilding from a clean output path passed. GitHub CI starts from a clean
checkout.

## Packaged macOS runtime receipt

The final `LorePia.app` was launched and inspected through macOS accessibility.
The main WebView used `tauri://localhost` and exposed the following text:

```text
LorePia
제품 코어에 연결되었습니다.
부트스트랩 계약: v1
코어 버전: 0.1.0
데이터 경계: 기기 로컬, 사용자가 선택한 LLM 요청만 외부 전송
가져온 실행 콘텐츠: M-1 검증 완료 전 비활성
```

The app was then quit normally. This proves the local packaged
Svelte-to-Tauri-to-Rust bootstrap round trip only.

## Android and supply-chain receipt

Android was not compiled locally because the host has no Android SDK. The
committed product wrapper is gated by GitHub CI. Its checked-in Wrapper JAR
hash is:

```text
e996d452d2645e70c01c11143ca2d3742734a28da2bf61f25c82bdc288c9e637
```

The wrapper points to Gradle `8.14.3-bin.zip` with the committed distribution
SHA-256
`bd71102213493060956ec229d946beee57158dbd89d0e62b91bca0fa2c5f3531`.
The product workflow also runs Gradle's wrapper-validation action before the
Android build.

## Interpretation

This receipt is not evidence of M0 completion or five-OS runtime support. The
cross-runner compile jobs become evidence only after the committed workflow is
run on GitHub, and physical-device smoke remains separate.
