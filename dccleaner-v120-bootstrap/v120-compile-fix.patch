--- a/app/src/main/java/com/dokpamo/dccleaner/CleanerForegroundService.java
+++ b/app/src/main/java/com/dokpamo/dccleaner/CleanerForegroundService.java
@@ -78,6 +78,7 @@
 
     private void runPreview(String id, String type, int maxItems) {
         TaskStore taskStore = new TaskStore(this);
+        SecureVault vault = new SecureVault(this);
         try {
             SecureVault.Session session = requireSession();
             taskStore.setState("PREVIEWING", "삭제 대상을 안전하게 읽는 중입니다.");
