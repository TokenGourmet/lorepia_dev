.DS_Store
node_modules/
build/
.svelte-kit/
.env
.env.*
!.env.example
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Tauri's schemas are regenerated from the locked command manifest. Android
# and Apple wrappers are product source and intentionally remain tracked.
src-tauri/gen/schemas/
